# TODO

- Add .bash_profile or .zsh_profile for theme customization
- Centralize keymaps for each plugin

# DEPENDENCIES

For python LSP:

``` bash
# Arch
sudo pacman -S pipx

pipx ensurepath

# MacOS
brew install pipx
pipx ensurepath
```

